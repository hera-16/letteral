# ==========================================
# 環境変数設定ファイル (本番環境用テンプレート)
# ==========================================
# このファイルをコピーして .env として保存し、実際の値を設定してください
# cp .env.example .env

# ------------------------------------------
# JWT Configuration (必須)
# ------------------------------------------
# 強力なランダム文字列を設定してください (64文字以上推奨)
# 生成方法: PowerShell: [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
JWT_SECRET=your-secure-random-jwt-secret-here-at-least-64-characters-long

# ------------------------------------------
# Database Configuration (本番環境では必須)
# ------------------------------------------
# 本番環境のMySQLサーバー接続情報
# SSLを有効化することを推奨: useSSL=true
SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/chatapp?useSSL=false&serverTimezone=Asia/Tokyo&allowPublicKeyRetrieval=true
SPRING_DATASOURCE_USERNAME=chatapp_user
SPRING_DATASOURCE_PASSWORD=chatapp_password

# ------------------------------------------
# CORS Configuration (本番環境では必須)
# ------------------------------------------
# フロントエンドのURLをカンマ区切りで指定
# 例: https://your-frontend-domain.com,https://www.your-frontend-domain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ------------------------------------------
# Server Configuration (オプション)
# ------------------------------------------
# サーバーポート (デフォルト: 8080)
SERVER_PORT=8080

# ------------------------------------------
# 本番環境デプロイ時の注意事項
# ------------------------------------------
# 1. JWT_SECRET: 必ず強力なランダム文字列を生成して設定してください
# 2. データベース: 本番環境のMySQL接続情報に変更してください
# 3. CORS: 実際のフロントエンドドメインに変更してください
# 4. SSL: 本番環境ではuseSSL=trueを推奨します
# 5. このファイル(.env)はGitにコミットしないでください (.gitignoreで除外済み)
